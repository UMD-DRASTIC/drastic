* Remove resource.filename column

> ALTER TABLE resource DROP file_name;
	
* Rename User.username to User.name in the schema
	
$ python migrations/user_username_to_name.py
> ALTER TABLE user DROP username;
(rename the field username in user.py when the columns has been migrated)

* Remove column Group.owner

> ALTER TABLE group DROP owner;

* Remove the indexes from the blob and blob_parts tables 

> DROP INDEX index_blob_parts;
> DROP INDEX index_blob_part_blob_id;

* Drop indexes on resource(size) and resource(id) 

> DROP INDEX index_resource_id;
> DROP INDEX index_resource_size;

* Drop index on IDindex(classname)

> DROP INDEX index_idindex_classname;

* Remove permission columns in Collection and Resource

> ALTER TABLE collection DROP read_access;
> ALTER TABLE collection DROP edit_access;
> ALTER TABLE collection DROP write_access;
> ALTER TABLE collection DROP delete_access;
> ALTER TABLE resource DROP read_access;
> ALTER TABLE resource DROP edit_access;
> ALTER TABLE resource DROP write_access;
> ALTER TABLE resource DROP delete_access;


* Use the interpreter to modify root collection

from indigo.models import initialise
initialise("indigo")
from indigo.models import Collection
root = Collection.get_root_collection()
root.update_acl(["AUTHENTICATED@"], [])



